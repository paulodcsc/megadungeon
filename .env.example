# Realtime sync (optional — without this the app only persists to localStorage).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Group passphrase (optional — without these auth is disabled).
# Set both to enable the login gate. Rotate SESSION_SECRET to invalidate all sessions.
APP_PASSPHRASE=
SESSION_SECRET=
